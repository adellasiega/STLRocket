numpy
torch>=2.0
aeon
python-glmnet
scikit-learn
scipy
pandas
matplotlib
seaborn

# torcheck is not on PyPI -- install separately from the local project, e.g.:
#   pip install -e ~/Projects/TorCheck
# (or wherever TorCheck is checked out on this machine)
